name: Run Linting and Infrastructures' Unit Tests

on:
  pull_request:
    paths: 
      - "**"

env:
  application-name: configure-aws-s3-website
  application-root: ${{ env.apllication-name }}/src

jobs:
  deploy:
    name: Run Terraform CDK Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Set environment
        run: |
          case ${GITHUB_REF##*/} in
            "production") echo "ENVIRONMENT=production" >> $GITHUB_ENV ;;
            "preprod") echo "ENVIRONMENT=preprod" >> $GITHUB_ENV ;;
            "qa") echo "ENVIRONMENT=qa" >> $GITHUB_ENV ;;
            *) echo "ENVIRONMENT=development" >> $GITHUB_ENV ;;
          esac

      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Install Node
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Install dependencies
        working-directory: ${{ env.apllication-root }}
        run: npm install

      - name: Install Terraform CDK
        run: npm install --global cdktf-cli@0.12.2

      # - name: Run ESLint
      #   working-directory: ${{ env.apllication-root }}
      #   run: npm run lint

      - name: Run Terraform CDK Unit Tests
        working-directory: ${{ env.apllication-root }}
        run: npm run test